server:
  port: 8888

spring:
  application:
    name: jobkonnect-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ranga-1993/jobkonnect-config-repo
          clone-on-start: true
          default-label: main

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
